"use client";

import ArticleListWrapper from "@/components/article/article-list-wrapper";
import React from "react";

type Props = {};

const Articles = (props: Props) => {
    return (
        <div>
            <ArticleListWrapper>abc</ArticleListWrapper>
        </div>
    );
};

export default Articles;
